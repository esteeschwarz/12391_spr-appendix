#print(xml_chsroot[8])
#children 3, replace works but content shit
xml_replace(xml_children(xml_children(xml_children(xml_children(sroot)))), .value="because" ,..., .where=3)
sroot<-scheme
print(xml_children(xml_children(xml_children(xml_children(sroot)))))
#print(xml_chsroot[8])
#children 3, replace works but content shit
xml_replace(xml_children(xml_children(xml_children(xml_children(sroot)))), .value="because" ,.where=3)
sroot<-scheme
print(xml_children(xml_children(xml_children(xml_children(sroot)))))
#clowess()
#download_xml("https://github.com/esteeschwarz/12431_hux2021-appendix/raw/12057_VS/hux2022/proverbs/package_hux2022_fragen_templateB_2022-02-04.xml","template.xml")
#getRS()
#getRS(guser="esteeschwarz",grepo="essais",gdir="main")
#getRs(file=NULL, guser='harrelfe', grepo='rscripts', gdir='raw/master',
#     dir=NULL, browse=c('local', 'browser'), cats=FALSE,
#    put=c('rstudio', 'source'))
#download.file("https://github.com/esteeschwarz/12431_hux2021-appendix/raw/12057_VS/hux2022/proverbs/items/GR1/context2022_items_GR01.csv","items.csv")
scheme<-read_xml("https://github.com/esteeschwarz/12431_hux2021-appendix/raw/12057_VS/hux2022/proverbs/package_hux2022_fragen_templateB_2022-02-04.xml")
print(xml_children(xml_children(xml_children(xml_children(sroot)))))
sroot<-scheme
print(xml_children(xml_children(xml_children(xml_children(sroot)))))
#print(xml_chsroot[8])
#children 3, replace works but content shit
xml_replace(xml_children(xml_children(xml_children(xml_children(sroot[19])))), .value="because")
print(xml_children(xml_children(xml_children(xml_children(sroot)))))
print(xml_children(xml_children(xml_children(xml_children(sroot[19])))))
print(sroot[2])
print(xml_children(sroot[2]))
print(xml_children(sroot)[2])
print(xml_children(xml_children(xml_children(xml_children(sroot))))[19])
#print(xml_chsroot[8])
#children 3, replace works but content shit
xml_replace(xml_children(xml_children(xml_children(xml_children(sroot))))[19], .value="<text>because</text>")
print(xml_children(xml_children(xml_children(xml_children(sroot))))[19])
print(xml_children(xml_children(xml_children(xml_children(sroot)))))
#print(xml_chsroot[8])
#children 3, replace works but content shit
xml_replace(xml_children(xml_children(xml_children(xml_children(sroot))))[19], .value=<text>because</text>)
#print(xml_chsroot[8])
#children 3, replace works but content shit
xml_replace(xml_children(xml_children(xml_children(xml_children(sroot))))[19], <text>because</text>)
#print(xml_chsroot[8])
#children 3, replace works but content shit
xml_replace(xml_children(xml_children(xml_children(xml_children(sroot))))[19], "<text>because</text>")
print(xml_children(xml_children(xml_children(xml_children(sroot)))))
print(xml_children(xml_children(xml_children(xml_children(sroot))))[18])
print(xml_children(xml_children(xml_children(xml_children(sroot))))[17])
print(xml_children(xml_children(xml_children(xml_children(sroot))))[[17]])
print(xml_children(xml_children(xml_children(xml_children(sroot))))[[17:1]])
print(xml_children(xml_children(xml_children(xml_children(sroot))))[[17:2]])
print(xml_children(xml_children(xml_children(xml_children(sroot))))[[17:1]])
print(xml_children(xml_children(xml_children(xml_children(sroot))))[[17[1]])
print(xml_children(xml_children(xml_children(xml_children(sroot)))))
print(xml_children(xml_children(xml_children(xml_children(sroot)))))
print(xml_children(sroot)[2])
print(xml_attr(sroot,"character",ns="section"))
print(sroot$doc)
xml_replace(newscheme1[28],.value="BOUNCE")
############################################# from here earlier state #######################
x <- read_xml('
<root>
<doc1 xmlns = "http://foo.com"><baz /></doc1>
<doc2 xmlns = "http://bar.com"><baz /></doc2>
</root>
')
xml_ns(x)
# When there are default namespaces, it's a good idea to rename
# them to give informative names:
ns <- xml_ns_rename(xml_ns(x), d1 = "foo", d2 = "bar")
ns
# Now we can pass ns to other xml function to use fully qualified names
baz <- xml_children(xml_children(x))
xml_name(baz)
xml_name(baz, ns)
xml_find_all(x, "//baz")
xml_find_all(x, "//foo:baz", ns)
str(as_list(x))
str(as_list(x, ns))
print(xml_children(xml_children(xml_children(xml_children(sroot))))[17[1]])
print(xml_children(xml_children(xml_children(xml_children(sroot))))[17[2]])
print(xml_children(xml_children(xml_children(xml_children(sroot))))[17[1]])
print(xml_children(xml_children(xml_children(xml_children(sroot))))[17[2]])
print(xml_children(xml_children(xml_children(xml_children(sroot))))[17[3]])
print(xml_children(xml_children(xml_children(xml_children(sroot))))[17[1]])
#print(xml_chsroot[8])
#children 3, replace works but content shit
xml_replace(xml_children(xml_children(xml_children(xml_children(sroot))))[19[1]], "<text>because</text>")
val<-"because"
#print(xml_chsroot[8])
#children 3, replace works but content shit
xml_replace(xml_children(xml_children(xml_children(xml_children(sroot))))[19[1]], val)
print(xml_children(xml_children(xml_children(xml_children(sroot)))))
val<-"<text>because</text>"
#print(xml_chsroot[8])
#children 3, replace works but content shit
xml_replace(xml_children(xml_children(xml_children(xml_children(sroot))))[19[1]], val)
#print(xml_chsroot[8])
#children 3, replace works but content shit
xml_replace(xml_children(xml_children(xml_children(xml_children(sroot))))[19[1]], val)
print(xml_children(xml_children(xml_children(xml_children(sroot)))))
print(xml_children(xml_children(xml_children(xml_children(sroot))))[19[1]])
val<-"text>because</text"
#print(xml_chsroot[8])
#children 3, replace works but content shit
xml_replace(xml_children(xml_children(xml_children(xml_children(sroot))))[19[1]], val)
print(xml_children(xml_children(xml_children(xml_children(sroot)))))
val<-"text>because"
#print(xml_chsroot[8])
#children 3, replace works but content shit
xml_replace(xml_children(xml_children(xml_children(xml_children(sroot))))[19[1]], val)
print(xml_children(xml_children(xml_children(xml_children(sroot)))))
#print(xml_chsroot[8])
#children 3, replace works but content shit
xml_replace(xml_children(xml_children(xml_children(xml_children(sroot))))[19[1[1]]], val)
#print(xml_chsroot[8])
#children 3, replace works but content shit
xml_replace(xml_children(xml_children(xml_children(xml_children(sroot))))[19[1[2]]], val)
#print(xml_chsroot[8])
#children 3, replace works but content shit
xml_replace(xml_children(xml_children(xml_children(xml_children(sroot))))[19[[1]]], val)
#print(xml_chsroot[8])
#children 3, replace works but content shit
xml_replace(xml_children(xml_children(xml_children(xml_children(sroot))))[19[[2]]], val)
#print(xml_chsroot[8])
#children 3, replace works but content shit
xml_replace(xml_children(xml_children(xml_children(xml_children(sroot))))[19[[[1]]]], val)
xml_replace(xml_children(xml_children(xml_children(xml_children(sroot))))[19[[[1]]]], val)
xml_replace(xml_children(xml_children(xml_children(xml_children(sroot))))[[19[[1]]]], val)
val<-c(because)
val<-c("because")
xml_replace(xml_children(xml_children(xml_children(xml_children(sroot))))[[19[[1]]]], val)
#i cant get replacement without / at the end of string... try vector
print(xml_children(xml_children(xml_children(xml_children(sroot)))))
pos<-1
xml_replace(xml_children(xml_children(xml_children(xml_children(sroot))))[[19[[1]]]], val, .where=1)
xml_replace(xml_children(xml_children(xml_children(xml_children(sroot))))[[19[[1]]]], val, 1)
#i cant get replacement without / at the end of string... try vector
print(xml_children(xml_children(xml_children(xml_children(sroot)))))
val<-c("text")
pos<-"dummy"
xml_replace(xml_children(xml_children(xml_children(xml_children(sroot))))[[19[[1]]]], val, 1)
#i cant get replacement without / at the end of string... try vector
print(xml_children(xml_children(xml_children(xml_children(sroot)))))
xml_replace(xml_children(xml_children(xml_children(xml_children(sroot))))[[19[[1]]]], val, pos)
#i cant get replacement without / at the end of string... try vector
print(xml_children(xml_children(xml_children(xml_children(sroot)))))
xml_replace(xml_children(xml_children(xml_children(xml_children(sroot))))[19], val, pos)
#i cant get replacement without / at the end of string... try vector
print(xml_children(xml_children(xml_children(xml_children(sroot)))))
val<-"text"
pos<-"dummy"
#val<-"text>because"
val<-"text"
pos<-"dummy"
#print(xml_chsroot[8])
#children 3, replace works but content shit
#print(xml_children(xml_children(xml_children(xml_children(sroot))))[19[1]])
xml_replace(xml_children(xml_children(xml_children(xml_children(sroot))))[19], val, pos)
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot)))))
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[19])
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[28])
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[27])
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[29])
#vector geht bis 28
#now try append after
xml_add_child(xml_children(xml_children(xml_children(xml_children(sroot)))), .where="after")
#vector geht bis 28
#now try append after
xml_add_child(xml_children(xml_children(xml_children(xml_children(sroot)))), val, .where="after")
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[29])
print(xml_children(xml_children(xml_children(xml_children(sroot)))))
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[29])
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[28])
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[28])
#vector geht bis 28
#now try append after
xml_add_sibling(xml_children(xml_children(xml_children(xml_children(sroot)))), val, .where="after")
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[28])
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[29])
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[27])
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[19])
print(xml_children(xml_children(xml_children(xml_children(sroot)))))
sroot<-scheme
print(xml_children(xml_children(xml_children(xml_children(sroot)))))
sroot<-scheme
#clowess()
#download_xml("https://github.com/esteeschwarz/12431_hux2021-appendix/raw/12057_VS/hux2022/proverbs/package_hux2022_fragen_templateB_2022-02-04.xml","template.xml")
#getRS()
#getRS(guser="esteeschwarz",grepo="essais",gdir="main")
#getRs(file=NULL, guser='harrelfe', grepo='rscripts', gdir='raw/master',
#     dir=NULL, browse=c('local', 'browser'), cats=FALSE,
#    put=c('rstudio', 'source'))
#download.file("https://github.com/esteeschwarz/12431_hux2021-appendix/raw/12057_VS/hux2022/proverbs/items/GR1/context2022_items_GR01.csv","items.csv")
scheme<-read_xml("https://github.com/esteeschwarz/12431_hux2021-appendix/raw/12057_VS/hux2022/proverbs/package_hux2022_fragen_templateB_2022-02-04.xml")
sroot<-scheme
print(xml_children(xml_children(xml_children(xml_children(sroot)))))
#vector geht bis 28
#now try append after
xml_add_child(xml_children(xml_children(xml_children(xml_children(sroot)))[28]), val, .where="after")
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[19])
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[28])
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[29])
#print(xml_chsroot[8])
#children 3, replace works but content shit
#print(xml_children(xml_children(xml_children(xml_children(sroot))))[19[1]])
xml_replace(xml_children(xml_children(xml_children(xml_children(sroot))))[29], val, pos)
#vector geht bis 28
#now try append after
xml_add_child(xml_children(xml_children(xml_children(xml_children(sroot)))[29]), val)
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[29])
#print(xml_chsroot[8])
#children 3, replace works but content shit
#print(xml_children(xml_children(xml_children(xml_children(sroot))))[19[1]])
xml_replace(xml_children(xml_children(xml_children(xml_children(sroot))))[29], val, pos)
#vector geht bis 28
#now try append after
xml_add_sibling(xml_children(xml_children(xml_children(xml_children(sroot)))[29]), val)
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[29])
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[28])
print(xml_children(xml_children(xml_children(xml_children(sroot)))))
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[28])
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[27])
print(xml_children(xml_children(xml_children(xml_children(sroot)))))
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[26])
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[20])
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[21])
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[22])
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[23])
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[24])
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[25])
#clowess()
#download_xml("https://github.com/esteeschwarz/12431_hux2021-appendix/raw/12057_VS/hux2022/proverbs/package_hux2022_fragen_templateB_2022-02-04.xml","template.xml")
#getRS()
#getRS(guser="esteeschwarz",grepo="essais",gdir="main")
#getRs(file=NULL, guser='harrelfe', grepo='rscripts', gdir='raw/master',
#     dir=NULL, browse=c('local', 'browser'), cats=FALSE,
#    put=c('rstudio', 'source'))
#download.file("https://github.com/esteeschwarz/12431_hux2021-appendix/raw/12057_VS/hux2022/proverbs/items/GR1/context2022_items_GR01.csv","items.csv")
scheme<-read_xml("https://github.com/esteeschwarz/12431_hux2021-appendix/raw/12057_VS/hux2022/proverbs/package_hux2022_fragen_templateB_2022-02-04.xml")
sroot<-scheme
#val<-"text>because"
val<-"text" #tag
pos<-"dummy" #text within tags
#vector geht bis 28
#now try append after
xml_add_sibling(xml_children(xml_children(xml_children(xml_children(sroot)))[28]), val, .where="after")
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[25])
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[26])
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[27])
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[28])
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[24])
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[25])
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[26])
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[27])
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[28])
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[29])
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[30])
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[31])
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[30])
#vector geht bis 28
#now try append after
xml_add_sibling(xml_children(xml_children(xml_children(xml_children(sroot)))[30]), val, .where="after")
#vector geht bis 28
#now try append after
xml_add_sibling(xml_children(xml_children(xml_children(xml_children(sroot)))[29]), val, .where="after")
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[31])
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[30])
#vector geht bis 28
#now try append after
xml_add_child(xml_children(xml_children(xml_children(xml_children(sroot)))[29]), val, .where="after")
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[30])
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[29])
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[28])
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[27])
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[26])
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[25])
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[24])
#vector geht bis 28
#now try append after
xml_add_child(xml_children(xml_children(xml_children(xml_children(sroot)))[30]), val, .where="after")
#vector geht bis 28
#now try append after
xml_add_child(xml_children(xml_children(xml_children(xml_children(sroot)))[29]), val, .where="after")
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[30])
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[31])
#vector geht bis 28
#now try append after
xml_add_child(xml_children(xml_children(xml_children(xml_children(sroot)))[30]), val, .where="after")
#vector geht bis 28
#now try append after
xml_add_child(xml_children(xml_children(xml_children(xml_children(sroot)))[29]), val, .where="after")
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[30])
#vector geht bis 28
#now try append after
xml_add_parent(xml_children(xml_children(xml_children(xml_children(sroot)))[29]), val, .where="after")
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[30])
#vector geht bis 28
#now try append after
xml_add_parent(xml_children(xml_children(xml_children(xml_children(sroot)))[30]), val, .where="after")
print(xml_children(xml_children(xml_children(xml_children(sroot)))))
#vector geht bis 28
#now try append after
xml_add_parent(xml_children(xml_children(xml_children(xml_children(sroot)))), val, .where="after")
print(xml_children(xml_children(xml_children(xml_children(sroot)))))
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[30])
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[31])
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[32])
#vector geht bis 28
#now try append after
xml_add_parent(xml_children(xml_children(xml_children(xml_children(sroot)))), val, .where="after")
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[32])
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[33])
xml_add_child(xml_children(xml_children(xml_children(sroot))), val, .where="after")
#i cant get replacement without / at the end of string... try vector
#works. with first tag, then text within tag
print(xml_children(xml_children(xml_children(xml_children(sroot))))[33])
print(xml_children(xml_children(xml_children(xml_children(sroot)))))
#clowess()
#download_xml("https://github.com/esteeschwarz/12431_hux2021-appendix/raw/12057_VS/hux2022/proverbs/package_hux2022_fragen_templateB_2022-02-04.xml","template.xml")
#getRS()
#getRS(guser="esteeschwarz",grepo="essais",gdir="main")
#getRs(file=NULL, guser='harrelfe', grepo='rscripts', gdir='raw/master',
#     dir=NULL, browse=c('local', 'browser'), cats=FALSE,
#    put=c('rstudio', 'source'))
#download.file("https://github.com/esteeschwarz/12431_hux2021-appendix/raw/12057_VS/hux2022/proverbs/items/GR1/context2022_items_GR01.csv","items.csv")
scheme<-read_xml("https://github.com/esteeschwarz/12431_hux2021-appendix/raw/12057_VS/hux2022/proverbs/package_hux2022_fragen_templateB_2022-02-04.xml")
sroot<-scheme
#val<-"text>because"
val<-"text" #tag
pos<-"dummy" #text within tags
print(xml_children(xml_children(xml_children(xml_children(sroot)))))
#vector geht bis 28
#now try append after
xml_add_child(xml_children(xml_children(xml_children(xml_children(sroot)))), val, .where="after")
print(xml_children(xml_children(xml_children(xml_children(sroot)))))
#vector geht bis 28
#now try append after
xml_add_parent(xml_children(xml_children(xml_children(xml_children(sroot)))), val, .where="after")
print(xml_children(xml_children(xml_children(xml_children(sroot)))))
#clowess()
#download_xml("https://github.com/esteeschwarz/12431_hux2021-appendix/raw/12057_VS/hux2022/proverbs/package_hux2022_fragen_templateB_2022-02-04.xml","template.xml")
#getRS()
#getRS(guser="esteeschwarz",grepo="essais",gdir="main")
#getRs(file=NULL, guser='harrelfe', grepo='rscripts', gdir='raw/master',
#     dir=NULL, browse=c('local', 'browser'), cats=FALSE,
#    put=c('rstudio', 'source'))
#download.file("https://github.com/esteeschwarz/12431_hux2021-appendix/raw/12057_VS/hux2022/proverbs/items/GR1/context2022_items_GR01.csv","items.csv")
scheme<-read_xml("https://github.com/esteeschwarz/12431_hux2021-appendix/raw/12057_VS/hux2022/proverbs/package_hux2022_fragen_templateB_2022-02-04.xml")
sroot<-scheme
#val<-"text>because"
val<-"text" #tag
pos<-"dummy" #text within tags
print(xml_children(xml_children(xml_children(xml_children(sroot)))))
print(xml_children(xml_children(xml_children(sroot)))))
print(xml_children(xml_children(xml_children(sroot))))
pos1<-xml_children(xml_children(xml_children(sroot[29])))
pos1<-xml_children(xml_children(xml_children(sroot[1:29])))
pos1<-xml_children(xml_children(xml_children(sroot)))
xml_add_sibling(xml_children(xml_children(xml_children(sroot[29]))), pos1, .where="after")
xml_add_sibling(xml_children(xml_children(xml_children(sroot[29]))), pos1[1:29], .where="after")
pos1[1:29]
pos1[29]
pos1[30]
xml_add_sibling(xml_children(xml_children(xml_children(sroot[29]))), .where="after")
xml_add_sibling(xml_children(xml_children(xml_children(sroot))), .where="after")
xml_add_sibling(xml_children(xml_children(xml_children(sroot))), pos1, .where="after")
pos1[30]
pos1[29]
print(xml_children(xml_children(xml_children(sroot))))
xml_add_child(xml_children(xml_children(sroot)), val, .where="after")
print(xml_children(xml_children(xml_children(sroot))))
pos1[63]
pos1[62]
print(xml_children(xml_children(xml_children(sroot)))[63])
print(xml_children(xml_children(xml_children(sroot)))[64])
print(xml_children(xml_children(xml_children(sroot)))[62])
xml_add_child(xml_children(xml_children(sroot)), pos1, .where="after")
print(xml_children(xml_children(xml_children(sroot))))
#clowess()
#download_xml("https://github.com/esteeschwarz/12431_hux2021-appendix/raw/12057_VS/hux2022/proverbs/package_hux2022_fragen_templateB_2022-02-04.xml","template.xml")
#getRS()
#getRS(guser="esteeschwarz",grepo="essais",gdir="main")
#getRs(file=NULL, guser='harrelfe', grepo='rscripts', gdir='raw/master',
#     dir=NULL, browse=c('local', 'browser'), cats=FALSE,
#    put=c('rstudio', 'source'))
#download.file("https://github.com/esteeschwarz/12431_hux2021-appendix/raw/12057_VS/hux2022/proverbs/items/GR1/context2022_items_GR01.csv","items.csv")
scheme<-read_xml("https://github.com/esteeschwarz/12431_hux2021-appendix/raw/12057_VS/hux2022/proverbs/package_hux2022_fragen_templateB_2022-02-04.xml")
sroot<-scheme
pos1<-xml_children(xml_children(xml_children(sroot)))
print(xml_children(xml_children(xml_children(sroot))))
xml_add_child(xml_children(xml_children(sroot)), pos1, .where="after")
print(xml_children(xml_children(xml_children(sroot))))
write_xml(sroot,"scheme_essai.xml")
